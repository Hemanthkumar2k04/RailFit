# =============================================================================
# RailFit Backend Production Requirements
# FastAPI-based railway asset management system - Production Optimized
# =============================================================================

# Core FastAPI and ASGI server (Production optimized)
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==21.2.0
python-multipart==0.0.12

# Data validation and settings (Synced with dev)
pydantic==2.11.7
pydantic-settings==2.6.1

# Environment and utilities
python-dotenv==1.0.1

# Database (Production: async optimized, but compatible versions)
sqlalchemy==2.0.32
alembic==1.13.2
# Production uses asyncpg for better async performance
asyncpg==0.30.0
# Keep psycopg for compatibility if needed
psycopg==3.2.3
psycopg-binary==3.2.3
greenlet==3.2.4

# Authentication and security (Synced versions)
PyJWT>=2.0,<3.0
bcrypt==4.3.0
passlib==1.7.4
python-jose==3.3.0
cryptography==46.0.1

# HTTP client and requests (Synced versions)
requests==2.32.3
httpx==0.27.2

# QR Code generation (Synced versions)
qrcode==7.4.2
pypng==0.20220715.0

# Image processing for ML model (Synced versions)
pillow>=11.0.0

# Machine Learning (Production: CPU-only TensorFlow for smaller footprint)
tensorflow-cpu>=2.20.0
keras>=3.11.0
numpy>=2.3.0
scikit-learn>=1.5.0

# Data processing and analysis
pandas>=2.2.0

# Supabase client (Synced versions)
supabase>=2.7.0
gotrue>=2.11.0
storage3>=0.7.0
realtime>=2.7.0
postgrest>=0.16.0

# Utilities and helpers (Synced versions)
click==8.2.1
python-dateutil==2.9.0.post0
pytz==2025.2
reportlab==4.0.7

# Production-specific: No watchfiles (dev-only hot reload)